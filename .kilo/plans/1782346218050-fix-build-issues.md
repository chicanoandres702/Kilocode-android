# Build Issues and Deprecations Fix Plan

## Status
- [x] Fixed `Icons.Rounded.ArrowBack` deprecation.
- [x] Fixed `MainActivity.kt` coroutine scope.
- [x] Updated GitHub Actions workflow.
- [x] Verified Android build with `./gradlew assembleDebug`.
- [x] Verified repository with `npm run typecheck` and `npm run lint`.

## Conclusion
All requested fixes are implemented and verified.
